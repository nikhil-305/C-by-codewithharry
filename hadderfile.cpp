#include<iostream>
//There are two types of header files;
// 1,System header files:Tt comes with the compiler
// 2.User difined headetr files;It is written by the programmer 
//#incude "this.h"//-->This  will produce an error if thsi.h is no present in the current directory

using namespace std;

int main(){
    int a=4,b=5;

    // cout<<"Operators in c++:"<<endl;
    // cout<<"following are the types of operators in c++";
    // Arithmetic Operator
    // cout<<"The value of a  +  b is "<< a+b<<endl;
    // cout<<"The value of a  -  b is "<< a-b<<endl;
    // cout<<"The value of a  *  b is "<< a*b<<endl;
    // cout<<"The value of a  /  b is "<< a/b<<endl;
    // cout<<"The value of a  %  b is "<< a%b<<endl;
    // cout<<"The value of a  ++  b is "<< a++<<endl;
    // cout<<"The value of a  --  b is "<< a--<<endl;
    // cout<<"The value of a  ++  b is "<< ++a<<endl;
    // cout<<"The value of a  --  b is "<< --a<<endl;
    // cout<<"The value of a  +  b is "<< a+b<<endl;

    // Assignment Operator --> used to assign values to variables
    // int a = 3, b=9;
    // char d='d';

    // comparison operator 
    // cout<<"following are the types of comparison operators in c++";
    // cout<<"The value of a ==b is "<<(a==b)<<endl;
    // cout<<"The value of a !=b is "<<(a!=b)<<endl;
    // cout<<"The value of a >=b is "<<(a>=b)<<endl;
    // cout<<"The value of a <=b is "<<(a<=b)<<endl;
    // cout<<"The value of a >b is "<<(a>b)<<endl;
    // cout<<"The value of a < is "<<(a<b)<<endl;

    //Logical operator
    cout<<"Following are the logical opeartor"<<endl;
    // cout<<"The value of these  logical and operator ((a==b)) && ((a<b)) "<<((a==b)) && ((a<b));
    // cout<<"The value of these  logical or operator ((a==b)) || ((a<b)) "<<((a==b)) || ((a<b)) ;
    //  cout<<"The value of these  logical operator (!(a==b)) "<<(!(a==b));




    return 0;
}
